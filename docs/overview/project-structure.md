# 專案結構說明

> 詳細的專案目錄結構與檔案說明

---

## 📁 頂層結構

```
gighub-master/
├── .github/              ← GitHub 配置與 Copilot 資源
├── .husky/               ← Git Hooks
├── .vscode/              ← VS Code 設定
├── .yarn/                ← Yarn 快取
├── _cli-tpl/             ← CLI 模板
├── _mock/                ← Mock 資料
├── docs/                 ← 專案文件
├── e2e/                  ← E2E 測試
├── plan/                 ← 規劃文件
├── public/               ← 公開資源
├── scripts/              ← 建置腳本
├── src/                  ← 原始碼
├── supabase/             ← Supabase 設定
└── package.json          ← 專案設定
```

---

## 📂 原始碼結構 (`src/`)

```
src/
├── app/
│   ├── core/                     ← 🔧 核心層
│   │   ├── facades/              ← Facades (API 封裝)
│   │   │   └── account/          ← 帳戶 Facade
│   │   ├── services/             ← 全域服務
│   │   └── startup/              ← 啟動設定
│   │
│   ├── shared/                   ← 📦 共用層
│   │   ├── components/           ← 共用元件
│   │   ├── directives/           ← 共用指令
│   │   ├── pipes/                ← 共用管道
│   │   └── utils/                ← 工具函數
│   │
│   ├── features/                 ← ✨ 功能模組
│   │   └── blueprint/            ← 藍圖功能
│   │       ├── data-access/      ← 資料存取層
│   │       │   ├── repositories/ ← Repository 模式
│   │       │   ├── services/     ← 服務
│   │       │   └── stores/       ← 狀態管理
│   │       ├── shell/            ← 外殼元件
│   │       └── ui/               ← UI 元件
│   │           ├── diary/        ← 日誌
│   │           ├── task/         ← 任務
│   │           └── todo/         ← 待辦
│   │
│   ├── routes/                   ← 🛣️ 路由
│   │   └── account/              ← 帳戶路由
│   │
│   └── layout/                   ← 🖼️ 佈局
│       ├── basic/                ← 基本佈局
│       └── fullscreen/           ← 全螢幕佈局
│
├── assets/                       ← 📁 靜態資源
│   ├── images/
│   └── styles/
│
├── environments/                 ← ⚙️ 環境設定
│   ├── environment.ts
│   └── environment.prod.ts
│
└── styles/                       ← 🎨 全域樣式
    └── theme.less
```

---

## 🏗️ 三層架構對應

| 架構層 | 目錄位置 | 說明 |
|--------|---------|------|
| **基礎層** | `core/facades/account/` | 帳戶、認證、組織 |
| **容器層** | `features/blueprint/shell/` | 藍圖容器、權限 |
| **業務層** | `features/blueprint/ui/` | 任務、日誌、待辦 |

---

## 📋 命名慣例

| 類型 | 命名規則 | 範例 |
|------|---------|------|
| 元件 | `kebab-case` | `task-tree.component.ts` |
| 服務 | `kebab-case` | `blueprint.service.ts` |
| Store | `kebab-case` | `task.store.ts` |
| 模型 | `PascalCase` | `Task.ts`, `Blueprint.ts` |
| 常數 | `UPPER_SNAKE_CASE` | `API_ENDPOINTS.ts` |

---

## 🔗 相關文件

- [系統架構](../architecture/system-architecture.md)
- [編碼標準](../reference/coding-standards.md)
- [Copilot 配置](../../.github/copilot/README.md)

---

**最後更新**: 2025-12-02
