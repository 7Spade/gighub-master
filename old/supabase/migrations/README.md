簡要說明

此目錄包含資料庫遷移檔（migrations），以有序、可追蹤的方式變更資料庫結構與資料（DDL / DML）。

- 位置：`supabase/migrations`
- 命名慣例：使用時間戳 + 簡短描述，例如 `20251129000001_create_tasks_table.sql`。
- 使用方式：請使用專案指定的遷移工具或 Supabase CLI 來套用/回滾遷移。不要直接編輯已套用於生產的歷史遷移檔，改為新增新的遷移檔。

版本與回滾：在建立遷移前請先在開發環境測試，並於 PR 中說明回滾步驟與相依性。
