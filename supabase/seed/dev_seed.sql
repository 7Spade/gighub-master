-- ============================================================================
-- Development Seed Data
-- Description: 開發環境種子資料
-- Created: 2024-12-01
-- ============================================================================

-- 種子資料應該包含測試用的基本資料，
-- 此檔案僅作為示例結構，實際資料應根據需求填充

-- 測試用戶帳號將由 Auth 自動建立
-- 組織和藍圖需要透過 API 函數建立

-- ============================================================================
-- 開發環境設定說明
-- ============================================================================
-- 1. 使用 Supabase Auth 建立測試用戶
-- 2. 透過 create_organization() 建立測試組織
-- 3. 透過 create_blueprint() 建立測試藍圖
-- 4. 手動或透過 API 建立測試任務/日誌等資料

-- ============================================================================
-- 可選：建立測試資料的函數
-- ============================================================================

-- 建立測試資料的函數（僅供開發環境使用）
CREATE OR REPLACE FUNCTION public.seed_dev_data()
RETURNS VOID
LANGUAGE plpgsql
SECURITY DEFINER
AS $$
BEGIN
  -- 此函數可用於建立開發測試資料
  -- 實際實作應根據需求進行
  RAISE NOTICE 'Development seed data function called';
END;
$$;

-- 授權給認證用戶執行（僅供開發環境）
GRANT EXECUTE ON FUNCTION public.seed_dev_data() TO authenticated;

-- ============================================================================
-- DOWN (Rollback)
-- ============================================================================
-- DROP FUNCTION IF EXISTS public.seed_dev_data();
